package com.mediatrack.app.repository;

import com.mediatrack.app.model.GlobalMedia;
import org.springframework.data.jpa.repository.JpaRepository;

public interface GlobalMediaRepository extends JpaRepository<GlobalMedia, Long> {}
